# 任务执行规范

防止复杂任务执行时卡住、过慢、过度分析。每次任务开始前快速阅读。
与 `AI_RULES.md`（开发红线，不可违反）配合使用；本文件关注"怎么执行"。


## 0. 会话启动（每次新对话必做）

新会话没有上次的记忆，必须先恢复上下文再动手：

1. 读 `AI_RULES.md` -- 16 条开发红线（不可违反）
2. 读留痕文档恢复上下文 -- 架构留痕 `docs/协作留痕.docx` 最后 5-8 条；若任务涉及特定国家的业务测试，读对应的 `docs/业务留痕-<国家>.docx` 尾部记录
3. 快速扫 `README.md` 阶段进度表 + 目录结构 -- 当前项目状态
4. 如果任务涉及特定模块，读该模块当前代码再动手

留痕文档是跨会话记忆。读尾部即可，不用读全文。

## 1. 快速定界（30 秒）

收到任务后先判断，不无差别读代码：

- 改动范围：单文件 / 多文件 / 跨模块 / 触碰 framework 核心
- 是否需要新测试或改测试
- 是否有现成模式可复用（先看同类文件怎么写的）

基于判断决定探索深度：单文件改动只读目标文件 + 相关测试；跨模块才扩大范围。

## 2. 并行探索

无依赖的操作合并到一个工具批次：

- 多个文件读取、`ls`、`git status`、`rg` 等，一次性并行
- 只有"后一步需要前一步的结果"时才串行等待
- 读 5 个文件用 1 个批次，不用 5 次串行

## 3. 假设优先，不反复确认

能用合理假设推进的，直接做，不问用户：

- 默认遵循代码库现有模式，不发明新抽象
- 命名、结构、测试风格对齐同类文件
- 仅当假设会导致不可逆的错误方向、或违反 `AI_RULES.md` 时才确认
- 用户没指定细节时，选与现有代码最一致的方案

## 4. 增量实现，边做边验

复杂任务拆步骤，每步完成后立即验证，不攒到最后：

- 改完代码 → 立即 `ruff check` / `mypy` / 跑相关测试子集
- 验证通过再进下一步
- 攒一堆改动最后一次性验证 = 错误堆积，定位成本指数上升

验证命令优先用子集（具体文件 / 具体 test），不全量跑，省时间。

## 5. 探索时间盒

- 探索阶段不超过 3-4 个工具调用就应开始动手
- 读了一个文件还没完全有头绪 → 先写最小可行实现，边写边修正
- 禁止"读完全部相关代码再开始"——读到足够动手就动手，细节边做边补

## 6. 卡住时的明确策略

| 情况 | 策略 |
| --- | --- |
| 命令失败 | 立即看报错、修、重跑，不绕过不跳过 |
| lint / 类型报错 | 当场修，不堆积 |
| 设计不确定 | 选最贴合现有模式的方案，直接做，不反复纠结 |
| 缺环境 / 缺权限 | 按沙箱规则请求提权，附 `justification` |
| 真正阻塞（需求矛盾等） | 简短告知具体阻塞点 + 2-3 个选项，不等用户回复就选最优继续 |

禁止的状态：长时间沉默、反复读同一批文件、用 `echo` / `cat` 拼凑大段代码、
"让我先理解一下"然后做十几次搜索。

## 7. 通信节奏

- 每 30 秒左右发一条 commentary 更新：在做什么、为什么，一两句话
- 不要等用户回复才继续推进（除非真正需要用户决策）
- 开始实质工作前，如果任务较大，用 `update_plan` 列步骤，增量更新状态

## 8. 修改前的影响分析

遵循 AI_RULES 第 9 条，改代码前快速回答三个问题：

1. 这个文件被谁 import / 谁依赖它？（`rg` 搜一下）
2. 改动会不会破坏既有接口签名？
3. 是否需要同步改测试？

不确定就说清楚假设，然后继续。

## 9. 提交卫生与文档同步

- 只改任务相关文件，不夹带无关重构
- 遇到他人未提交的改动：不动、不回退，绕开继续
- 不用 `git reset --hard` / `git checkout --` 除非用户明确要求
- 任务完成后检查关联文档是否需要同步：
  - `README.md` -- 目录结构/阶段进度/使用说明有变化时更新
  - `docs/协作留痕.docx`（架构）或 `docs/业务留痕-<国家>.docx`（业务） -- 按第 10 条格式追加记录
  - 模块 `__init__.py` 的 `__all__` 导出列表
  - 新增公共 API 时更新对应模块文档


## 10. 协作留痕

每次完成实质任务后，必须向对应留痕文档追加记录。文档包含表格结构，不只写文字段落。

### 留痕文档分类

| 文档 | 用途 | 适用阶段 |
| --- | --- | --- |
| `docs/协作留痕.docx` | 框架架构搭建记录 | 架构/框架开发 |
| `docs/业务留痕-<国家>.docx` | 各国家业务测试记录 | 业务测试（按国家独立编号） |

- 架构留痕：记录 framework 开发、工具链、CI/CD、规范等框架级工作
- 业务留痕：按国家拆分，用户告知国家后记录到对应文档（如 `业务留痕-埃及.docx`、`业务留痕-德国.docx`）
- 每个业务留痕文档独立编号，从对话 1 开始
- 新增国家时创建对应的 `docs/业务留痕-<国家>.docx`，结构复制已有业务留痕

### 必须更新的内容

1. **对话索引表**（Table 1）：追加一行 -- 轮次 / 日期 / 主题 / 状态
2. **对话正文**：标题段落 + 正文段落

```
【用户】 <标题> · 对话 N · <日期>
<用户请求摘要>

【助手】 <响应类型> · 对话 N · <日期>
<交付摘要：做了什么、关键决策>
```

3. **文件变更表**（有文件改动时必须附）：文件 / 说明 / 状态(新增/修改/重构/更新)
4. **验证结果表**（跑过检查时必须附）：检查项 / 命令 / 结果
5. **影响范围分析表**（触碰共享代码时必须附）：维度 / 说明

### 规则

- 对话编号接续**当前文档**中最大编号，不跳号
- 日期用 `YYYY-MM-DD`
- 响应类型：响应 / 交付 / 执行结果 / 核查结果 / 体检结论 等，按实际性质选
- 纯闲聊/问候不记录；实质性的需求、决策、交付都记录
- 无文件变更的对话（纯阅读/讨论）可省略文件变更表，但索引表必须更新
