# Production environment for jmsco (哥伦比亚 JMS system).
# Credentials must come from env vars / .env, never hardcoded (rule 10).
env: prod
debug: false
log_level: WARNING
web:
  base_url: https://jmsco.prod
  browser: chromium
  headless: true
  timeout_ms: 30000
