[project]
name = "loadtest"
version = "0.1.0"
description = "接口压测项目 (Locust + framework 场景驱动)"
requires-python = ">=3.12"
dependencies = [
    "framework",
    "locust>=2.20",
]

[tool.uv.sources]
framework = { workspace = true }
