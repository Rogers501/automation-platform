[pytest]
minversion = 8.0
testpaths = testcase
asyncio_mode = auto
pythonpath = .
addopts = -ra --strict-markers

markers =
    smoke: critical-path tests run on every commit (fast gate).
    regression: comprehensive tests run in the regression stage.