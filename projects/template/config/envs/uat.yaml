# UAT environment.
env: uat
debug: false
log_level: INFO
http:
  base_url: https://uat-api.example.com
database:
  type: postgresql
  host: uat-db.example.com
  database: template_uat
  username: template
  # password supplied via APP_DATABASE__PASSWORD or .env (never hardcoded)